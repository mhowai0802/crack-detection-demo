# Unified install for local development — use per-side files for production.
# Backend: pip install -r backend/requirements.txt
# Frontend: pip install -r frontend/requirements.txt

-r backend/requirements.txt
-r frontend/requirements.txt

# Extras only used by scripts/notebooks (train, prepare_data, evaluate CLI).
pandas>=2.0.0
matplotlib>=3.8.0
